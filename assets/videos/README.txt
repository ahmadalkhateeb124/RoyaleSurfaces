Put about.mp4 here.

It renders in the "Why Royale Surfaces" section on the homepage, in place of
about-warehouse.jpg. While no file is present the photo shows instead, so the
page never breaks.

  - Portrait, roughly 800x1000 (the column is taller than it is wide)
  - Under ~8 MB, H.264 / yuv420p, no audio track
  - It plays silently on loop with no controls

about-warehouse.jpg stays as the poster frame — it is what shows for a moment
before the video decodes, and what someone with "reduce motion" enabled sees.
